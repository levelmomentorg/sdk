namespace LevelMomentIntegration
{
    // Production access pairing is managed by the Level Moment SDK.
    // Do not add a production token, endpoint, or password to the game.
    internal static class LevelMomentAccess
    {
        internal const bool UsesSdkManagedAccess = true;
    }
}

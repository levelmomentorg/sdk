// Reads the LevelMoment student token from the app's launch URL.
// Wire this to your deep-link / route parameters.

Future<String> getTokenFromUrl() async {
  // LEVELMOMENT_TODO: replace with your routing solution. Examples:
  //
  //   // go_router:
  //   return GoRouterState.of(context).uri.queryParameters['token'] ?? '';
  //
  //   // uni_links:
  //   final uri = await getInitialUri();
  //   return uri?.queryParameters['token'] ?? '';
  return '';
}

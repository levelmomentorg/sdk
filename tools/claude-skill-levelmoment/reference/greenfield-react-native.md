# Add a React Native break to a supplied slot

Read [the React Native port guide](port-react-native.md). Add a break only at
the natural pause slot named in the handoff. Do not infer a new pause point.
